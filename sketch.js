
var jaxon

function setup(){
  createCanvas(400,400);
  jaxon = createSprite (200,250, 20, 20);
  jaxon.AddImage("Runner-1.png")
}

function draw() 
{
  background(0);

  if (keyIdDown(a) {

    jaxon.AddImage("Runner-1.png")
    jaxon.velocityX = 2
  }

  if (keyIdDown(d) {

    jaxon.AddImage("Runner-2.png")
    jaxon.velocityX = -2
  }
}
